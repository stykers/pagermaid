[Unit]
Description=PagerMaid telegram utility daemon
After=network.target

[Install]
WantedBy=multi-user.target

[Service]
Type=simple
User=pagermaid
WorkingDirectory=/var/lib/pagermaid
ExecStart=/bin/bash -c "source venv/bin/activate; /usr/bin/env python3 -m pagermaid"
Restart=always